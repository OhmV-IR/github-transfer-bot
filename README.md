In .env, add the following variables:
- DISCORD_TOKEN is the bot token (NOT THE OAUTH2 CLIENT SECRET), it's in the bot tab.
- GITHUB_TOKEN is the github PAT or fine grained access token
- CLIENT_ID is the discord bot oauth2 client id
